# 推流故障排查决策树

本文档覆盖 10 个常见推流故障场景，每个场景按「判定条件 → 排查步骤 → 解决方案」组织，确保每条路径 ≤3 步完成排查。

> 💡 **每个场景顶部均提供「快速解决（≤3 步）」区块**，列出最高命中率动作；若仍未解决，再阅读下方完整排查流程。

## 快速索引

| # | 场景 | 典型症状 |
|---|------|----------|
| 1 | [RTMP地址失效](#1-rtmp地址失效) | OBS提示连接失败/URL无效 |
| 2 | [防火墙拦截](#2-防火墙拦截) | 网络正常但推流超时 |
| 3 | [编码器崩溃](#3-编码器崩溃) | OBS推流中突然停止/闪退 |
| 4 | [平台限流](#4-平台限流) | 推流成功但画质被压缩/断流 |
| 5 | [分辨率不匹配](#5-分辨率不匹配) | 平台显示黑边/画面拉伸变形 |
| 6 | [音频设备冲突](#6-音频设备冲突) | 推流画面正常但无声/杂音 |
| 7 | [多平台密钥过期](#7-多平台密钥过期) | 部分平台推流失败，其余正常 |
| 8 | [网络抖动丢帧](#8-网络抖动丢帧) | 观众端卡顿/马赛克但本地流畅 |
| 9 | [CPU过载降帧](#9-cpu过载降帧) | OBS显示"编码过载"/帧率下降 |
| 10 | [推流中断自动重连失败](#10-推流中断自动重连失败) | 断流后无法自动恢复 |

---

## 1. RTMP地址失效

> **快速解决（≤3 步）**
> 1. 登录平台后台**重新生成推流地址和密钥**（视频号每次开播地址不同）。
> 2. 确认地址以 `rtmp://` 开头，服务器和推流码分别填入对应字段，无多余空格。
> 3. 检查平台官方状态页是否在维护，必要时切换备用推流服务器地址。

**症状**：OBS提示"连接服务器失败"或"无效的URL"。

**判定条件**：推流地址是否在有效期内？（部分平台地址有时效性）

```
地址是否过期？
├── 是 → 步骤1
└── 否 → 地址格式是否正确（rtmp://开头）？
    ├── 否 → 步骤2
    └── 是 → 步骤3
```

**排查步骤**：

1. **重新获取推流地址**：登录直播平台后台，重新生成推流地址和密钥（视频号每次开播地址不同）。
2. **检查地址格式**：确认地址以 `rtmp://` 开头，服务器地址和推流码分别填入对应字段，无多余空格。
3. **验证平台服务状态**：访问平台官方状态页或社群确认是否在维护，更换备用推流服务器地址测试。

---

## 2. 防火墙拦截

> **快速解决（≤3 步）**
> 1. 在系统防火墙/路由器/企业网关中放行 **TCP 1935 出站**，或临时关闭防火墙测试。
> 2. 终端执行 `telnet <推流服务器> 1935`，超时即确认被拦截。
> 3. 切换到手机热点推流，若正常则为当前网络策略问题。

**症状**：本地网络可正常上网，但OBS推流超时（连接时间>30s后失败）。

**判定条件**：其他网络应用是否正常？推流端口(1935)是否放行？

```
浏览器能正常打开网页吗？
├── 否 → 非防火墙问题，检查网络连接
└── 是 → 端口1935是否被拦截？
    ├── 是 → 步骤1
    └── 不确定 → 步骤2
```

**排查步骤**：

1. **放行RTMP端口**：在系统防火墙/路由器/企业网关中添加 TCP 1935 出站规则，或临时关闭防火墙测试。
2. **测试端口连通性**：终端执行 `telnet <推流服务器地址> 1935`，若超时则确认被拦截，联系网管开放端口。
3. **切换网络环境**：使用手机热点推流测试，若热点下正常则确认为当前网络策略问题。

---

## 3. 编码器崩溃

> **快速解决（≤3 步）**
> 1. 将编码器从硬件 (NVENC/AMF) 切换为软件 (x264)，或更新显卡驱动。
> 2. 降低输出分辨率至 1280x720、码率至 2500kbps，排除资源耗尽。
> 3. 重装最新版 OBS 并逐一禁用第三方插件，定位冲突插件。

**症状**：推流过程中 OBS 突然停止推流或程序闪退。

**判定条件**：OBS日志是否显示编码器相关错误？

```
OBS日志中是否有"encoder error"或"nvenc"错误？
├── 是（GPU编码器问题）→ 步骤1
└── 否 → 是否频繁在固定时间崩溃？
    ├── 是 → 步骤2
    └── 否 → 步骤3
```

**排查步骤**：

1. **切换编码器**：将编码器从硬件(NVENC/AMF)切换为软件(x264)，或更新显卡驱动至最新版本。
2. **降低编码负载**：降低输出分辨率至1280x720、码率至2500kbps，排除资源耗尽导致的崩溃。
3. **更新OBS版本**：卸载后重新安装最新版OBS，同时检查插件兼容性（逐一禁用第三方插件定位问题插件）。

---

## 4. 平台限流

> **快速解决（≤3 步）**
> 1. 调整码率至平台上限内（抖音 ≤6000、淘宝 ≤4000、视频号 ≤6000 kbps）。
> 2. 检查账号粉丝数与认证是否满足平台推流资质要求。
> 3. 联系平台客服核查风控规则，提交申诉解除限制。

**症状**：推流成功但观众端画质明显低于设置值，或平台强制断流。

**判定条件**：当前码率是否超出平台限制？账号是否有推流资质限制？

```
推流码率是否超过平台上限？
├── 是 → 步骤1
└── 否 → 账号是否满足推流资质要求？
    ├── 否 → 步骤2
    └── 是 → 步骤3
```

**排查步骤**：

1. **调整码率至平台限制内**：抖音建议≤6000kbps，淘宝≤4000kbps，视频号≤6000kbps。在OBS"输出"设置中修改比特率。
2. **确认账号推流资质**：检查粉丝数是否达标（抖音200有效粉丝、视频号100粉丝），完成平台要求的认证。
3. **联系平台客服**：可能触发平台风控规则（频繁开关播、异常时段推流），提交申诉解除限制。

---

## 5. 分辨率不匹配

> **快速解决（≤3 步）**
> 1. 按平台方向设置画布：竖屏 1080x1920，横屏 1920x1080，输出分辨率与之同步。
> 2. OBS 来源右键 → 变换 → "适配屏幕"，避免手动拉伸。
> 3. 在平台开播设置中选择匹配的画质档位（4000-6000kbps）。

**症状**：平台直播画面出现黑边、拉伸变形或裁切不完整。

**判定条件**：OBS输出分辨率与平台要求是否一致？

```
平台是否要求竖屏(9:16)推流？
├── 是 → OBS画布是否设为竖屏？
│   ├── 否 → 步骤1
│   └── 是 → 步骤2
└── 否（横屏16:9）→ 步骤3
```

**排查步骤**：

1. **设置竖屏画布**：OBS → 设置 → 视频 → 基础分辨率改为 1080x1920（或720x1280），输出分辨率同步调整。
2. **检查缩放过滤器**：OBS来源右键 → 变换 → 选择"适配屏幕"，避免手动拉伸导致比例失调。
3. **匹配平台推荐分辨率**：横屏统一使用1920x1080输出，码率4000-6000kbps，在平台开播设置中选择对应画质选项。

---

## 6. 音频设备冲突

> **快速解决（≤3 步）**
> 1. OBS → 设置 → 音频，将麦克风/桌面音频改为**具体设备**（非"默认"），断开重连。
> 2. 确认推流音频轨道包含麦克风+桌面音频，且未被静音。
> 3. 关闭其他占用音频设备的程序，启用"噪声抑制"和"噪声门"滤镜。

**症状**：推流画面正常但观众听不到声音，或出现电流声/重复回声。

**判定条件**：OBS音频混音器是否有电平跳动？

```
OBS混音器中麦克风是否有绿色电平跳动？
├── 否（无信号）→ 步骤1
└── 是 → 观众端是否能听到？
    ├── 否 → 步骤2
    └── 听到但有杂音/回声 → 步骤3
```

**排查步骤**：

1. **重新选择音频设备**：OBS → 设置 → 音频 → 确认麦克风/桌面音频选择了正确的设备（非"默认"），断开再重新连接设备。
2. **检查音频轨道映射**：OBS → 输出 → 确认推流使用的音频轨道(Track)包含麦克风和桌面音频，未被静音或映射到未推流的轨道。
3. **消除冲突源**：关闭其他占用音频设备的程序（如语音通话软件），在OBS中对麦克风启用"噪声抑制"和"噪声门"滤镜。

---

## 7. 多平台密钥过期

> **快速解决（≤3 步）**
> 1. 登录失败平台后台**重新获取地址+密钥**，更新 Multi RTMP 配置。
> 2. 检查该平台账号是否被封禁/限流，必要时重新授权。
> 3. 确认上行带宽足够（每路码率 +20% 冗余），否则降码率或减少同推路数。

**症状**：使用 Multi RTMP 插件同时推多个平台，部分平台推流失败而其余正常。

**判定条件**：失败平台的推流码/密钥是否仍然有效？

```
失败的平台是否需要每次开播更新密钥？
├── 是（如视频号）→ 步骤1
└── 否 → 单独测试该平台地址是否可用？
    ├── 不可用 → 步骤2
    └── 可用 → 步骤3
```

**排查步骤**：

1. **更新过期密钥**：登录对应平台后台重新获取推流地址和密钥，更新Multi RTMP插件中对应目标的配置。
2. **验证账号状态**：确认该平台账号未被封禁/限流，登录平台App检查直播权限是否正常，必要时重新授权。
3. **检查插件并发限制**：确认网络带宽是否足够支撑多路推流（每路至少需要预留码率+20%冗余），可降低单路码率或减少同推平台数。

---

## 8. 网络抖动丢帧

> **快速解决（≤3 步）**
> 1. 将码率降至上传带宽的 70% 以下，并改用有线连接替代 WiFi。
> 2. OBS → 输出 → 勾选"动态码率调整"（OBS 28+）。
> 3. 更换就近 CDN 节点，或联系 ISP 排查上行链路。

**症状**：本地OBS显示正常帧率但观众端出现卡顿、马赛克、花屏。

**判定条件**：OBS底部状态栏是否显示"丢帧"(dropped frames)？

```
OBS状态栏丢帧率是否>0%？
├── 是 → 丢帧率是否>5%？
│   ├── 是 → 步骤1
│   └── 否 → 步骤2
└── 否 → 步骤3
```

**排查步骤**：

1. **降低码率并稳定网络**：码率降至网络上传带宽的70%以下（如上传10Mbps则码率设≤7000kbps），改用有线连接替代WiFi。
2. **启用动态码率**：OBS → 输出 → 勾选"动态码率调整"（OBS 28+），让编码器根据网络状况自动调节。
3. **检查CDN节点**：更换推流服务器地址为就近节点（部分平台提供多个服务器可选），或联系ISP排查上行链路质量。

---

## 9. CPU过载降帧

> **快速解决（≤3 步）**
> 1. 关闭高 CPU 占用程序（游戏录像、多标签浏览器、后台更新）。
> 2. 编码预设从 "slow" 改为 "veryfast"，或切换到 GPU 硬件编码 (NVENC/AMF)。
> 3. 减少激活来源、关闭多余滤镜，画布帧率从 60fps 降为 30fps。

**症状**：OBS显示红色"编码过载"警告，输出帧率低于设定值。

**判定条件**：任务管理器中CPU占用是否持续>90%？

```
CPU占用是否>90%？
├── 是 → 是否有其他高CPU占用程序？
│   ├── 是 → 步骤1
│   └── 否 → 步骤2
└── 否 → 步骤3
```

**排查步骤**：

1. **关闭非必要程序**：结束游戏录像、浏览器（尤其是多标签页）、后台更新等高CPU占用进程，仅保留OBS和直播必需软件。
2. **降低编码压力**：将编码预设从"慢(slow)"改为"非常快(veryfast)"，或切换到GPU硬件编码(NVENC/AMF)分担CPU负载。
3. **优化OBS场景**：减少同时激活的来源数量，关闭不必要的视频滤镜（色彩校正、锐化等），将画布帧率从60fps降为30fps。

---

## 10. 推流中断自动重连失败

> **快速解决（≤3 步）**
> 1. OBS → 设置 → 高级，勾选"自动重新连接"，重试延迟 5 秒、最多 20 次。
> 2. 若平台显示"直播已结束"，到平台重新开播并更新 OBS 推流地址。
> 3. 用 `ping` 监测网关丢包，必要时更换网络或联系 ISP。

**症状**：网络短暂中断后OBS未能自动重新连接，或重连后平台显示"直播已结束"。

**判定条件**：OBS是否启用了自动重连？平台是否支持断流续推？

```
OBS是否开启了自动重连？
├── 否 → 步骤1
└── 是 → 重连后平台是否显示"直播已结束"？
    ├── 是 → 步骤2
    └── 否（一直重连失败）→ 步骤3
```

**排查步骤**：

1. **启用自动重连**：OBS → 设置 → 高级 → 勾选"自动重新连接"，设置重试延迟为5秒、最大重试次数为20次。
2. **平台断流超时处理**：部分平台断流超过30-60秒会自动关闭直播间。需在平台重新开启直播并获取新推流地址，然后在OBS中更新地址后重新推流。
3. **排查根本断流原因**：检查网络是否周期性中断（路由器过热/ISP不稳定），使用 `ping` 持续监测网关丢包情况，必要时更换网络环境或联系ISP。

---

## 通用排查技巧

- **查看OBS日志**：帮助 → 日志文件 → 上传当前日志，获取详细错误信息。
- **使用OBS自动配置向导**：工具 → 自动配置向导，让OBS根据硬件和网络自动推荐设置。
- **测试推流**：先推流至测试平台（如自建RTMP服务器或B站测试），确认本地无问题后再切换正式平台。
